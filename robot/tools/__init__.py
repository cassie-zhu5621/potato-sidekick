"""robot.tools — Standalone diagnostics. Run them directly; nothing imports them.

  check_bus.py       find the adapter, ping servos, watch a joint live
  jog.py             drive one joint by hand -- how calibration.py was measured
  preflight.py       GO / NO-GO before a session
  check_clips.py     validate the CSVs against the calibrated limits
  play_on_hardware.py play one clip
  breath_test.py     LED envelope only
"""
